# Copy to .env and fill with your own credentials. NEVER commit the real .env.
OPENAI_API_KEY=
HF_TOKEN=
# Optional (ablation Telegram notifications)
TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=
# Azure Content Safety: provide via azure_config.json (gitignored), not here.
# Gemini / Vertex AI (imgen_gemini.py): use `gcloud auth application-default login` + --project.
